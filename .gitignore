# raw takes and exports never belong in git — see tenderfy-video/README.md
footage/
exports/
*.mp4
# ...except the one clip the film actually needs at runtime
!scene1-office.mp4
*.mov
*.webm
animatic.v03.backup.html
